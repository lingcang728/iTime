# Dependencies & build output
node_modules/
dist/
coverage/
.vite/
src-tauri/target/
src-tauri/gen/

# Local package / scratch output (never commit binaries)
release/
output/
visual-output/
artifacts/
*.log
*.tmp
*.exe
*.msi
*.dmg
*.AppImage

# Local agent / AI tooling notes (keep on disk, never publish)
CLAUDE.md
claude.md
.claude/
.omo/
.agents/
.codex/
.codegraph

# Local product docs / design sources (keep on disk, do not publish)
iTime蓝图_实施版.md

# Browser-capture reference is versioned with the visual baselines.
!iTime原型图.png
