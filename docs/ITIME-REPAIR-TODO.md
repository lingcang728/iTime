# iTime 0.1.1 修复与发布进度

更新日期：2026-07-27

执行来源：`C:\Users\15pro\Desktop\iTime-修复计划-2026-07-26.md`

当前状态：阶段 0–5 已完成，阶段 6 进行中

本文是当前修复工作的唯一仓库内进度入口。上一轮已完成的审计闭环已归档到
`docs/archive/ITIME-REPAIR-TODO-2026-07-18.md`。

## 阶段 0：真实基线与可恢复进度

- [x] 建立本轮仓库内 TODO，并保留上一轮已完成记录。
- [x] 分支与提交：`main` / `702d69065c727d7903a9d783e301618fe0fc6b5f`。
- [x] 远端：`origin/main` 与本地一致；远端没有 `v0.1.1`。
- [x] 开始时工作树干净，没有混入用户改动。
- [x] 工具版本：Node `24.15.0`、npm `11.12.1`、rustc/cargo `1.92.0`。
- [x] 复用全局 Playwright `G:\python\Scripts\playwright.exe`，浏览器缓存
  `G:\build_cache\playwright-browsers`；未在项目重复安装。
- [x] 基础门禁通过：25 个前端测试文件、97 项前端测试、39 项 Rust 测试、
  TypeScript/Vite、rustfmt、Clippy 全部通过。
- [x] `npm run verify:full` 真实复现失败；证据保存在本地
  `artifacts/visual/layout-report.json` 与同目录截图中。

基线失败：

1. `inputModeMotion=false`：柱状模式的实际过渡为 `300ms`，低于规范要求的
   `550ms`，不是测试误报。
2. `focusIndicators.light.weeklyTrend.activeVisible=false`：浅色主题焦点确实进入
   周趋势点，描边对比度为 `5.93:1`，但焦点目标留在滚动视口外；深色主题同项可见。

## 阶段 1：完整门禁与发布可信度（P0）

### 1.1 视觉门禁

- [x] 输入模式切换使用 `600ms` cubic-bezier 过渡。
- [x] `prefers-reduced-motion: reduce` 立即切换，并由 `inputReducedMotion` 单独覆盖。
- [x] 周趋势浅色/深色键盘焦点进入后均在滚动视口内且清晰可见。
- [x] `npm run verify:full` 在同一环境连续通过两次。

阶段 1.1 验证证据（2026-07-27）：

- 两次完整门禁均通过：26 个前端测试文件 / 99 项测试、39 项 Rust 测试、
  TypeScript/Vite、rustfmt、Clippy、交互矩阵和视觉回归。
- `inputModeMotion`、`inputReducedMotion`、`inputRangeMotion`、浅色/深色
  `weeklyTrend` 焦点断言均为 `true`。
- 根因修复：输入模式过渡从 300ms 提升为 600ms；周趋势 SVG 圆点动画不再用
  `r: inherit` 把最终半径归零，改为保持 `r` 属性的 scale 动画。

### 1.2 可复现打包

- [x] `package:release` 强制运行 `verify:full` 并重新构建。
- [x] 打包过程不写开始菜单、`HKCU\App Paths` 或其他用户系统入口。
- [x] 同一构建同步生成便携版和安装包，且 `release` 中恰有两个 EXE。
- [x] 生成 `release/release-manifest.json`，记录版本、提交、时间、文件大小和 SHA-256。
- [x] 两个 release 文件分别与 Cargo/NSIS 同轮源产物哈希一致。

阶段 1.2 验证证据（2026-07-27）：

- 默认 `npm run package:release` 完成 `verify:full`、fresh Tauri/NSIS 构建、
  双 EXE 原子替换和 `verify-release-manifest.ps1` 独立复核。
- 默认 release 产物：便携版 `10,244,608` 字节，安装包 `2,443,728` 字节；
  manifest 中每个目标哈希与 Cargo/NSIS 构建源哈希一致。
- 打包前后 `HKCU\App Paths` 值、快捷方式长度、修改时间和 SHA-256 完全相同。
- 在此前不存在的 `output/stage1-zero-release` 中再次成功生成两个 EXE 和 manifest，
  证明从零路径不依赖旧 release 文件；该目录作为本地忽略证据保留。
- 曾遇到一次嵌套 Windows PowerShell 无法自动加载 `Get-FileHash`，已改用无模块依赖的
  .NET SHA-256 文件流；一次旧便携版进程锁由精确 `ExecutablePath`/PID 识别后仅关闭该实例。

### 1.3 CI 与不可变发布

- [x] Windows PR/main CI 覆盖 `npm ci`、`verify:full` 和生产依赖审计。
- [x] 视觉失败证据可上传，但不包含用户运行数据。
- [x] `v*` 发布工作流验证版本一致、干净重建、manifest 和不可变标签。
- [ ] 官网下载元数据由同一 manifest 生成或校验。

阶段 1.3 当前证据（2026-07-27）：

- CI 与发布工作流使用官方 Actions v7 的固定 commit；Windows CI 同时执行锁定安装、
  生产依赖审计和完整视觉门禁，失败时只上传合成测试页面截图与报告。
- `audit:prod` 显式使用 npm 官方安全审计端点，避免本机安装镜像不实现审计 API 时产生
  “无漏洞”的假通过或与代码无关的 404。
- 发布仅响应 `v*` 标签，校验 package/package-lock/Cargo/Tauri 四处版本、标签指向、
  干净工作树和 manifest；同标签已有 GitHub Release 时直接失败。
- 发布只创建两个 EXE 资产，随后从 GitHub Release 重新下载并按同一 manifest 独立验哈希。
- GitHub Windows CI run `30231348848` 在干净 runner 上通过锁定安装、生产依赖审计、
  26 个前端文件 / 99 项测试、TypeScript/Vite、Clippy、39 项 Rust 测试和完整视觉门禁。
- 指标真实性改动后曾遗漏同步首页等视觉基线，后续运行因此继承失败；同步人工复核后的
  28 张基线后，又暴露时间线 tooltip 用固定 `140ms` 等待与 `140–150ms` CSS 过渡竞速。
  视觉脚本现等待真实 opacity 就绪，Windows CI run `30234649807` 已在干净 runner 上
  完整通过，未关闭或放宽任何门禁。
- 官网 manifest 对接将在阶段 5 与网站修订一起完成。

## 阶段 2：统计真实性与 Provider 隐私（P0）

- [x] 统一指标定义，移除硬编码比较、静态微柱、固定时段和夸大语义。
- [x] 缺少数据或比较基线时显示明确缺失状态。
- [x] Provider 使用版本化显式授权；未授权时零扫描。
- [x] Codex/Claude 独立开关和一次性权限说明。
- [x] 只解析必要时间/事件元数据，不访问、保存或输出正文。
- [x] 修复运行中区间缓存、候选选择、容量上限、淘汰和分级错误报告。
- [x] 覆盖未授权、坏行、权限失败、文件删除、4096+ 候选和时间异常测试。

阶段 2.1 验证证据（2026-07-27）：

- 新增共享指标定义，记录名称、公式、单位、自然日窗口和缺失状态；首页、时间线、
  AI 页和键盘页复用同一比较与说明口径。
- 首页/时间线不再出现固定“较昨日”值；缺少上一日时显示“暂无对比”，缺少当前值时
  显示“当前暂无数据”，不会把 `null` 转成 0%。
- 前台应用切换按相邻身份 A→B 计数，相邻同应用片段不重复计数；前台专注占比使用
  前台专注时长除以设备活动时长。
- 周报微柱、最长时段、夜间前台活跃均来自真实事件桶/区间；没有上周数据时不再改用
  “同周其他日”制造提升结论。
- `MetricCard` 只有收到真实 `progress` 时才显示圆环，没有趋势序列时不再渲染装饰性柱条。
- 侧栏改由实际来源状态和最后刷新时间驱动；浏览器数据明确标为“预览数据”。
- 本机 `verify:full` 通过：29 个前端文件 / 106 项测试、39 项 Rust 测试、构建、格式、
  Clippy、视觉与交互矩阵全部通过；人工检查首页、周报、AI、键盘四张宽屏截图无布局退化。

阶段 2.2–2.3 验证证据（2026-07-27）：

- 设置 schema 升级为版本 2，Provider 授权记录独立使用版本 1；从旧设置升级时 Codex 和
  Claude Code 均默认关闭，授权命令在解析 `USERPROFILE` 前短路，未授权测试确认候选数、
  扫描数、区间数和缓存数全部为 0。
- 设置页首次只显示一次性读取边界说明；确认说明本身不会启用扫描，随后才显示 Codex 与
  Claude Code 两个独立开关。页面明确列出目录、字段、用途和关闭后停止扫描/清除缓存的影响。
- Provider JSONL 改用只声明时间、类型、时长、role 和 stop reason 的强类型结构；
  `message/content` 不在结构中，也不再使用 `serde_json::Value` 或正文路径。超大且结构异常的
  content 边界测试仍能解析必要元数据，序列化输出不含正文。
- 缓存只保存完成区间和未完成起点等不可变事实；运行中区间每次快照用当前时间重算。
  候选选择先遍历全部合格元数据再选最新 2048 个，缓存只保留当前候选集；删除和替换文件
  都会使旧事实失效。
- 快照分别报告坏行、坏事件、普通读取失败和权限失败；关闭、无目录、无权限、损坏和部分
  可用有不同状态或用户文案。单文件混合坏行仍保留正常区间。
- Provider 专项覆盖 11 项 Rust 测试，包括 5000 个候选、运行区间推进、删除/替换、
  未来时间、时钟回拨与跨日；设置迁移/校验另有 2 项测试。
- 完整本机 `verify:full` 通过：30 个前端文件 / 108 项测试、49 项 Rust 测试、构建、
  rustfmt、Clippy、视觉与交互矩阵全部通过。浅色、深色和滚动后的设置页截图经人工复核；
  仅更新 4 张有意变化的设置页基线，未放宽视觉阈值。
- 远端 run `30231856818` 曾因 hash 路由竞态截到只有侧栏的首张首页图；后续同页截图完整。
  视觉脚本现同时等待目标 hash 与目标页唯一结构，避免把上一页残留 `.page` 当作就绪。

## 阶段 3：原生采集与 Windows 生命周期（P0）

- [x] 暂停/继续通过控制通道即时生效，命令时间戳截断片段并隔离采集代次。
- [x] 后端确认后前端才更新录制状态；失败回滚。
- [x] 键盘事件携带采集代次，连续输入周期落盘，队列丢弃可观测。
- [x] 写盘同步、有限重试、坏行恢复和退出刷新。
- [x] Ctrl/Alt/Win 组合不计数，Shift 字符键计数。
- [x] 用户界面统一称为“字符键按下次数”。
- [x] 安装器与便携版注册所有权分离；应用启动和打包不抢写系统入口。
- [x] COM 初始化/反初始化严格配对。
- [x] 自启动状态刷新只读，只有用户明确切换时写入。

阶段 3 验证证据（2026-07-27）：

- 活动采集器改为有确认回执的控制通道；暂停以命令时间戳关闭当前片段，继续创建新
  generation，相同应用也不会跨暂停区间合并。UI 只在后端确认后完成切换；失败会恢复
  旧设置与 hook 原子状态，并显示单一错误。
- 键盘事件在 hook 时刻固定记录启用状态、时间戳和 generation；Ctrl/Alt/Win 组合被排除，
  Shift 字符键保留。独立 deadline 在持续输入中周期刷新，队列溢出、写入/读取失败和断连
  均进入健康状态；写入执行三次有界重试、`flush` 与 `sync_data`，退出前强制刷新。
- 键盘专项覆盖持续输入、队列满、写盘失败、退出刷新、坏行恢复、左右修饰键和暂停代次；
  活动专项覆盖精确暂停边界、快速切换、跨代不合并、空闲边界和睡眠/挂起间隔。
- 应用启动只设置进程级 AppUserModelID，不再写注册表或快捷方式。NSIS 安装钩子仅在
  App Paths 为空或已有本安装器所有权标记时写入；卸载同时核对所有权与精确安装路径。
  Tauri 自带开始菜单清理继续使用目标路径验证，便携版不自注册。
- COM 回收测试确认 `S_OK`、`S_FALSE` 都配对 `CoUninitialize`，失败结果不反初始化。
  自启动刷新只调用一次 `isEnabled`，`enable`/`disable` 只在用户明确切换时调用并有 pending。
- 真实 `tauri build`/makensis 成功生成安装包；构建前后当前用户 App Paths 与开始菜单
  快捷方式快照完全不变。
- 完整本机 `verify:full` 通过：31 个前端测试文件 / 113 项测试、58 项 Rust 测试、
  TypeScript/Vite、rustfmt、Clippy、交互矩阵和视觉回归全部通过。

## 阶段 4：时间线、UI、无障碍和本地数据（P1）

- [x] 时间线使用本地日期，默认 `00:00–24:00`，范围按钮真实生效。
- [x] 当前时间只在范围内显示，热区按真实区间交集计算。
- [x] 清理假按钮和竞态；异步状态覆盖 loading/empty/degraded/error/ready。
- [x] 输入趋势图使用 roving tabindex、方向键、Home/End、Enter/Space。
- [x] 图表提供屏幕阅读器摘要或等价表格。
- [x] 设置页支持打开数据目录、JSON/CSV 导出、二次确认清空和保留期。
- [x] JSONL 轮转、原子写入、损坏恢复和安全清理。
- [x] 图标缓存键包含身份/尺寸/解析器版本，并按容量/LRU 清理。

阶段 4.1–4.3 当前证据（2026-07-27）：

- 时间线日期判断改用本地年月日，不再通过 UTC 字符串判断“今天”；默认范围为本地自然日
  `00:00–24:00`，全天/工作时段按钮会真实改变坐标、刻度和片段裁剪。当前时间不属于所选
  本地日期或范围时返回空，不再钳制到轨道边缘。
- 时间线模型测试覆盖全天、工作时段、23:59、非当天和范围外当前时间；轨道继续按片段与
  选择范围的真实交集裁剪，跨边界片段不会仅按开始时间错误丢失。
- `App.vue` 的桌面事件监听逐项注册和清理，一个监听失败不会丢弃此前成功的监听；
  首页、AI 页和周报没有行为定义的“查看全部/排序/视图”按钮已改为静态说明。
- Rust 提醒运行时生成唯一 occurrence ID；系统通知、toast 和首页卡片共享同一 ID，
  已关闭 ID 有界持久化。旧的前端独立计时与 occurrence 计算已删除。
- 输入趋势图只有一个数据点进入 Tab 顺序，支持方向键、Home/End 和 Enter/Space；
  同一数据另有仅屏幕阅读器可见的表格摘要。组件测试覆盖焦点移动和选择。
- 本机 `npm run verify` 通过：32 个前端测试文件 / 116 项测试、58 项 Rust 测试、
  TypeScript/Vite、rustfmt 和 Clippy 全部通过。完整视觉矩阵、深浅主题、DPI、滚动、
  焦点和 29 项交互断言通过；人工复核首页、AI、时间线、输入和周报宽屏截图。

阶段 4.4 当前证据（2026-07-27）：

- 活动和键盘记录改为按本地日期分片，并在单片达到 16 MiB 后继续写入编号分片。每条记录
  先完整序列化，再单次写入、刷新并同步到磁盘；意外中断留下的不完整尾行会被隔离，之后
  的完整记录仍可读取。
- 旧版单文件迁移采用可恢复的中间文件和逐行计数去重；迁移中断后可继续，不会重复记录。
  无法解析的旧行会保留到 `Data/Recovery`，正常记录进入对应日期分片。
- 保留期支持永久（升级和默认均不删除）、365 天和 90 天。清理只处理可识别的历史分片，
  跳过当天文件，不会碰导出和恢复目录。
- 设置页显示真实目录、记录范围、最近写入、文件数量和体积；JSON/CSV 导出写入临时文件、
  同步后原子替换，并由测试重新读取验证记录数、时长和按键计数。CSV 不含窗口标题、路径、
  按键内容或按键身份。
- 清空操作需要二次确认，后端再次校验固定确认文本；整个暂停、关闭区间、键盘刷新、删除和
  恢复过程持有记录状态锁。失败时不清除前端数据，浏览器预览也不会伪造本机文件操作。
- 本机 `npm run verify:full` 通过：33 个前端测试文件 / 118 项测试、71 项 Rust 测试、
  TypeScript/Vite、rustfmt、Clippy、交互矩阵和视觉回归全部通过；28 张回归截图差异为 0。

阶段 4.5 当前证据（2026-07-27）：

- Rust 队列的进行中请求和失败冷却均按应用身份、规范化尺寸和解析器版本隔离；同一应用的
  32px 与 128px 请求不再相互吞掉。可执行路径提示更新时只清除该身份的全部尺寸失败记录。
- 前端内存缓存、进行中 Promise 和事件回填使用同一复合键；组件只接收与当前身份和当前
  请求尺寸同时匹配的结果，尺寸变化期间到达的旧事件不会覆盖新图标。
- 磁盘缓存保留身份、来源路径、来源修改时间、尺寸和解析器版本键；命中会更新访问时间，
  写入采用临时文件、完整写入、刷新、同步和原子改名。损坏 PNG 会删除，缓存超过 32 MiB
  时按最久未使用顺序清理，并保护本次刚写入的文件。
- 测试覆盖尺寸/版本隔离、边界尺寸归一化、损坏文件失效和最旧项淘汰；本机
  `npm run verify:full` 通过：34 个前端测试文件 / 120 项测试、77 项 Rust 测试、
  TypeScript/Vite、rustfmt、Clippy、交互矩阵和视觉回归全部通过，28 张截图差异为 0。

阶段 4.6 当前证据（2026-07-27）：

- 活动、字符键和 Provider 数据源都在每次读取开始时进入 `loading`；成功但没有记录进入
  `empty`，部分坏记录或丢弃事件进入 `degraded`，采集器/写入器停止、权限失败或调用异常
  进入 `error`，有完整可用记录才进入 `ready`。
- 浏览器 `preview` 和 Provider `disabled` 继续作为明确的非本机采集状态，不会伪装成五态
  中的成功结果。首页、时间线、AI、键盘、周报、目标、设置和侧栏分别显示对应状态文案。
- 状态测试覆盖空记录、错误、首次加载和已有数据后台刷新；本机 `npm run verify:full`
  通过：34 个前端测试文件 / 122 项测试、77 项 Rust 测试、TypeScript/Vite、rustfmt、
  Clippy、交互矩阵和视觉回归全部通过，28 张截图差异为 0。

## 阶段 5：文档、网站、依赖和许可证

- [x] 官网从 manifest 或 Release API 获得版本、大小和 SHA-256。
- [x] 更新隐私说明、下载链接验证、canonical/OG URL 和移动菜单行为。
- [x] 生产依赖审计为 CI 阻断，开发依赖逐项审慎升级。
- [x] 许可证由所有者确认；若发布前仍未确认，删除 README/网站“开源”声明。

许可证状态：发布前仍未收到许可证选择；按计划保持无许可证，并已删除 README/网站
的“开源”承诺，明确“源代码可见不等于授予复制、修改或再分发权”。

阶段 5.3 当前证据（2026-07-27）：

- Windows CI 和不可变发布工作流都在完整验证前执行 `npm run audit:prod`，显式使用 npm
  官方安全审计端点；生产依赖审计为 0 个漏洞。
- `vue-tsc` 从 2.2.12 升级到 3.3.8，移除旧 `@vue/language-core` / `minimatch` 告警。
  `@vue/test-utils` 保持当前 2.4.11，只把其声明的 `js-beautify` 精确覆盖到 2.0.3；
  对应 `editorconfig`、`glob`、`minimatch` 和 `brace-expansion` 均进入已修复版本。
- 完整 npm 审计从 8 个开发链高危降为 0；Node 24.15.0 下 `npm run verify:full` 通过
  34 个前端测试文件 / 122 项测试、77 项 Rust 测试及全部视觉门禁，截图差异为 0。

阶段 5.1/5.2 当前证据（2026-07-27）：

- 官网通过 GitHub 最新正式 Release API 读取版本、发布时间、两个资产的文件名、大小、
  下载链接和 GitHub `sha256:` digest；API 或元数据校验失败时只回退到 Releases 页面，
  不展示旧版本或未经验证的哈希。
- `npm run verify:website` 自动检查 Release 对接、下载与 digest 绑定、站内锚点、外部
  HTTPS 链接、canonical/绝对 OG URL、移动菜单 Escape/点击外部关闭及缓存头。
- 隐私说明已同步 Provider 逐项授权、仅解析时间/事件元数据、字符键本机计数和
  导出/保留期/清空控制；固定文件名资源取消 `immutable`。
- 本地 HTTP 真实浏览器验收读取到 GitHub `v0.1.0` 的两个资产、大小与 digest，
  390×844 视口下 Escape 和点击正文均关闭移动菜单。随后 `npm run verify:full`
  再次通过 34 个前端测试文件 / 122 项测试、77 项 Rust 测试及全部视觉门禁；
  五张官网产品截图只从该次零差异视觉基线同步，目标 SHA-256 与来源逐一相等。

## 阶段 6：原生 QA 与 0.1.1 发布

- [x] `npm ci`、`npm run verify:full`、生产依赖审计和全部新增测试通过。
- [ ] 真实 Tauri/WebView2 覆盖首次启动、托盘、退出、暂停/继续、键盘、
  Provider、提醒、自启动、数据管理和异常恢复。
- [ ] 安装版/便携版注册所有权矩阵通过。
- [x] package/npm/Cargo/Tauri 版本统一为 `0.1.1`。
- [ ] `npm run package:release` 生成本轮两个 EXE 和有效 manifest。
- [ ] 完整 diff 只提交源码与配置，排除本地产物。
- [ ] 分片提交全部推送，远端分支与本地一致。
- [ ] 创建不可变 `v0.1.1` 标签和 GitHub Release，仅上传本轮两个 EXE。
- [ ] 官网重新下载资产后独立核对 SHA-256。
- [ ] 最终工作树干净。

阶段 6 当前证据（2026-07-27）：

- 从干净依赖状态完成 `npm ci`，完整 npm 审计与生产依赖审计均为 0 个漏洞。
  `npm run verify:full` 通过 34 个前端测试文件 / 122 项测试、77 项 Rust 测试、
  TypeScript/Vite、网站检查、rustfmt、Clippy、交互矩阵与 28 张零差异视觉截图。
- package、package-lock、Cargo.lock、Cargo.toml 与 Tauri 配置的版本均为 `0.1.1`。
- 新增 `npm run test:native`，复用本机共享 Python Playwright，不在项目安装浏览器或运行时。
  它以隔离的本地数据和 WebView2 目录启动真实 Release EXE，真实调用 Tauri IPC，并确认
  暂停/继续后端回执、Provider 首次默认关闭且扫描文件数为 0、90 天与永久保留期、
  JSON/CSV 原子导出、错误删除确认被拒绝、正确清空且保留导出、提醒开关、设置页原生表面、
  关闭提示、隐藏到托盘和恢复窗口全部通过。
- 本机 WebView2 `150.0.4078.99` 的固定 CDP 端口不监听；QA 改用官方支持的
  `--remote-debugging-port=0`，从隔离数据目录读取临时端口，并只在
  `ITIME_NATIVE_QA=1` 时启用发布构建检查器。验收后精确停止本轮 EXE、删除隔离运行目录，
  确认调试端口关闭，便携版前后的 App Paths、开始菜单和自启动快照完全相同。
- 真实安装、升级、便携路径含空格、卸载后保留便携版、最终重装与自启动恢复仍在本阶段
  后半段执行；正式干净 manifest、远端 CI、标签、Release 和官网下载复核尚未提前勾选。

## 约束与延期

- 不改写 `v0.1.0`。
- 不把 SQLite/WAL、签名、自动更新、跨平台和 CSP 深化混入本轮。
- 不在未确认时擅自选择许可证。
- 每个独立问题域完成后更新本文的状态、验证证据和剩余风险。
